# Security Model

The canonical repository-wide security model is [litefold-calendar threat model](litefold-calendar-threat-model.md). It defines assets, actors, attacker-controlled inputs, trust boundaries, invariants, realistic attacker stories, accepted limitations, and severity calibration.

This file is retained as a stable pointer for security-policy and contributor links. Update the canonical threat model whenever a runtime surface, trust boundary, dependency policy, or release path changes.
